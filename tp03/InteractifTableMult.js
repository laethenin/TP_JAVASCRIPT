let nombre = 3;

console.log ("Table de " + nombre);

// édite la table de multiplication (de 1 à 10) pour un nombre
for (let i = 1; i <=10; i++) {
    let multiplication = nombre*i;
    console.log (nombre + "*" + i + "=" + multiplication);
}

